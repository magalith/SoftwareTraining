from database.models import User, Class

# 管理员接口，更新班级信息
def upgrade_class(dir_post):

    for i in dir_post:
        # 更新class.name  class.room
        i['class_name']
        i['room']
        # 更新教室班级id
        for k in i['teacher']:

        # 更新学生班级id
        for j in i['students']:




